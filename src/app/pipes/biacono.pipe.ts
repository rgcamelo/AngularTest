import { Pipe, PipeTransform } from '@angular/core';
import { ActivityObject } from '../models/activities';

@Pipe({
  name: 'biacono'
})
export class BiaconoPipe implements PipeTransform {

  transform(soy: any, quiero: boolean): any {


  }

}
